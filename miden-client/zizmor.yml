rules:
  use-trusted-publishing:
    ignore:
      - publish-crates-release.yml
  secrets-outside-env:
    ignore:
      - publish-crates-dry-run.yml
      - publish-crates-release.yml
      - trigger-deploy-docs.yml

[package]
name = "sealed-tests"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
sealed-sdk = { path = "../sdk/sealed-sdk" }
sealed-account = { path = "../contracts/sealed-account" }
